item: H-UPD-1
date: 2026-04-12T09:47:33Z
severity: medium
description: PR-thread pushback is not currently machine-actionable for the orchestrator/reviewer loop. I replied on the review thread to explain why `brew upgrade ninthwave` is still correct for `brew install ninthwave-sh/tap/ninthwave`, but that comment alone did not appear to create a durable pushback state; the practical workaround is pushing another commit to retrigger automation. This creates ambiguity about the intended mechanism for disagreement or clarification. A dedicated `nw` command, keyworded PR comment format, or structured inbox/file-based pushback path would make reviewer disagreement auditable and automatable without requiring a no-op commit.
