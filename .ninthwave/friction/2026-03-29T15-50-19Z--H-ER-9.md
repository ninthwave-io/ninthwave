item: H-ER-9
date: 2026-03-29T17:00:00Z
severity: medium
description: Context window exhaustion mid-implementation required session continuation via compact summary. On resume, the branch was 5 commits behind origin/main due to 5 PRs merging while work was in progress. A stash-pop conflict (H-ER-7 changed orchestrate.ts in the same region being extracted) required manual conflict resolution and subsequent H-ER-7 changes had to be re-applied to snapshot.ts. The double-rebase workflow (rebase → stash → pop → conflict → resolve → commit → rebase again) is error-prone for long-running extraction refactors.
